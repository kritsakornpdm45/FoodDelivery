export interface ArrowProps {
    className?: string;
    style?: React.CSSProperties;
    onClick?: () => void;
  }

export interface IFourthprops {
    id?: number;
    image: string;
    title: string;
    location: string;
    price: number;
}
